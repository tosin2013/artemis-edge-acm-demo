= Artemis Edge ACM Demo
:page-layout: home

== Overview

ifndef::mode_multi_hub[]
This demo showcases a federated AMQ Broker messaging architecture deployed across RHACM-managed Single Node OpenShift (SNO) edge clusters. Edge brokers use AMQP federation to route regional messages (`messages.NY.#`, `messages.NJ.#`, `messages.CT.#`) back to hub brokers with store-and-forward resilience.
endif::[]

ifdef::mode_multi_hub[]
This demo is Mode 2: a hub-of-hubs control plane plus regional AMQ. You log into **regional ACM east**. Site brokers on SNOs federate to the **AMQ hub** (`hub-01` on this cluster) using address classes 5603–5607. Global Hub is inventory and compliance only — no AMQ broker, and sites never talk to it.
endif::[]

=== Architecture

ifndef::mode_multi_hub[]
* *Hub Cluster* -- RHACM hub running ArgoCD, Keycloak OIDC, hub AMQ Brokers, Prometheus, and Grafana
* *Edge Clusters* -- SNO clusters (NY, NJ, and optionally CT). RHACM Governance (`edge-broker-policies`) installs the AMQ Broker operator and one `ActiveMQArtemis` per region after join
* *Messaging* -- AMQP federation from each SNO broker to hub-01 (`messages.NY.#`, `messages.NJ.#`, `messages.ALL.#`) over an AMQPS Route
endif::[]

ifdef::mode_multi_hub[]
You are on a *Tier 1 regional ACM hub* (east). AgnosticD already created **Tier 0 Global Hub** plus three regional ACM hubs. You create **Tier 2 SNOs** in xref:module-02.adoc[Module 2]. Sites never talk to Global Hub.

.Vocabulary
[cols="1,3"]
|===
| Term | Meaning

| ACM hub
| This cluster — regional ACM east (`local-cluster`)

| AMQ hub
| Broker `hub-01` on this regional ACM hub. There is no AMQ broker on Global Hub.

| Global Hub
| Tier 0 inventory and policy compliance only

| Site
| A SNO you join in Module 2. Sites never talk to Global Hub.
|===

.Mode 2 control plane — you are on regional ACM east
image::mode2-control-plane.png[Mode 2 three-tier control plane. You are here is marked on ACM east. Global Hub has inventory, compliance Grafana, and fleet Thanos Query. SNOs join east only.,link=self,window=blank,width=800]
endif::[]

=== Deployment Mode

ifdef::mode_multi_hub[]
You are running *Mode 2*: 1 Global Hub + 3 regional ACM hubs (east/central/west). This terminal is the **east** regional hub. SNOs are not present until Module 2.
endif::[]
ifndef::mode_multi_hub[]
You are running *Mode 1: Single Hub* with 1 RHACM hub and student-provisioned SNO edge clusters.
endif::[]

=== Getting Started

Open the *Terminal 1* tab on the right and log in to your OpenShift cluster:

[source,bash,role=execute,subs="attributes+"]
----
{login_command}
----

Verify you are logged in:

[source,bash,role=execute]
----
oc whoami
----

You should see `{user}`. You now have read access to all cluster resources used in this demo.

TIP: Your credentials are `{user}` / `{password}`. The OpenShift Web Console is available at https://console-openshift-console.{openshift_cluster_ingress_domain}[Console ↗].

=== Console Links

ifdef::mode_multi_hub[]
[cols="1,2"]
|===
| Component | URL

| AMQ Console (regional AMQ hub)
| {amq_console_url}

| Keycloak Admin
| {keycloak_url}

| Grafana (this region's MCO)
| {grafana_url}

| RHACM Console (this ACM hub)
| {rhacm_console_url}
|===
endif::[]

ifndef::mode_multi_hub[]
[cols="1,2"]
|===
| Component | URL

| AMQ Console (Hub)
| {amq_console_url}

| Keycloak Admin
| {keycloak_url}

| Grafana Dashboard
| {grafana_url}

| RHACM Console
| {rhacm_console_url}
|===
endif::[]
